{
  "name": "grt-vesting-contracts-supply",
  "license": "MIT",
  "scripts": {
    "test": "rm -rf ./tests/.bin  && npx graph test index",
    "codegen": "npx graph codegen",
    "build": "npx graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ gabrielcamba/grt-vesting-contracts-supply",
    "deploy:dev": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ gabrielcamba/subgraphito",
    "deploy-studio": "graph deploy --node https://api.studio.thegraph.com/deploy/ grt-vesting-contracts-supply --ipfs https://api.thegraph.com/ipfs/",
    "create-local": "graph create --node http://localhost:8020/ gabrielcamba/grt-vesting-contracts-supply",
    "remove-local": "graph remove --node http://localhost:8020/ gabrielcamba/grt-vesting-contracts-supply",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 gabrielcamba/grt-vesting-contracts-supply"
  },
  "devDependencies": {
    "@graphprotocol/contracts": "^1.1.0"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.23.2",
    "@graphprotocol/graph-ts": "0.23.1",
    "@protofire/subgraph-toolkit": "0.1.2",
    "matchstick-as": "0.2.0"
  }
}
